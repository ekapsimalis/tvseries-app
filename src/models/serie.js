/*
    Series Organizer - Database Models
    Model: Serie
    Base Serie model to mirror the API response
*/

const mongoose = require('mongoose')

const Serie = mongoose.model('Serie', {

})

module.exports = Serie